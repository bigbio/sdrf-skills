# PXD054083 — SDRF annotation report

**Status:** ANNOTATED · 186 rows × 43 columns · validated (parse_sdrf 0.1.5 exit 0, 0 ERRORs, 1 justified warning)

> **DUPLICATE — read first.** Every one of PXD054083's 186 deposited raw runs is **byte-for-byte a
> member of the already-annotated `PXD054083 ⊂ PXD051942`**. PXD054083 is the study's **"non-EB"
> subset submission** (HeLa / HEK293T / hFF arms only; the 1,190 iPSC/embryoid-body runs live only in
> PXD051942). Recorded as **Group 6** in `annotations/DUPLICATES.md`. A consumer concatenating both
> SDRFs would **double-count all 186 cells/bulk samples**. This SDRF is a faithful, independently
> re-verified annotation of *this* accession; the annotation of the 186 shared runs is intentionally
> kept **consistent with the approved PXD051942 rows** rather than re-derived divergently.

## Study and publication
- **Title:** *Global analysis of protein turnover dynamics in single cells* (SC-pSILAC) — identical to PXD051942.
- **Authors / lab:** Sabatier P, Lechner M, Guzmán UH, Beusch CM, … Ye Z, **Olsen JV** (NNF Center for Protein Research, Copenhagen / Karolinska).
- **Publication:** *Cell* 2025, **PMID 40168994**, DOI 10.1016/j.cell.2025.03.002 (**not** open access). bioRxiv preprint **DOI 10.1101/2024.05.30.596745** (green OA; read in full for Methods — fetched via Unpaywall to scratch and text-extracted). PRIDE `publications` list is **empty** (as for the sibling); the paper was confirmed by title + author + protocol match.
- **Instrument / workflow (from PRIDE `sample_processing_protocol` + preprint Methods):** Evosep One (40 SPD, 31-min gradient), Aurora Elite TS column → **Orbitrap Astral**, narrow-window DIA; full MS 240 000 res, scan range 380–980 m/z, 4 Th isolation window, 6 ms max IT, **HCD 27 % NCE**. Analysis: **Spectronaut 18, directDIA+**, 2-channel SILAC (channel1 = light/no mod; channel2 = Lys8 + Arg10), UniProt 2022 (20 588 seq) + 246 contaminants, **carbamidomethylation explicitly NOT included**, cross-run normalization removed.

This is **pulsed SILAC within single cells**: within one run the light channel is pre-existing protein and the heavy channel is newly synthesised protein. It is **not** isobaric multiplexing — **1 raw file = 1 biological sample** → annotated **one row per raw file, no channel→sample map required** (per brief).

## Templates chosen and why
`ms-proteomics` (MS base) · `human` (Homo sapiens) · `cell-lines` (HeLa/HEK293T/hFF are cultured lines; licenses `cellosaurus accession`/`cellosaurus name`) · `single-cell` (cellenONE-sorted 1-cell runs; licenses `sample type`, `cells per well`, `single cell isolation protocol`, `cell identifier`) · `dia-acquisition` (nDIA; licenses the scan-window/isolation-window columns and pins acquisition to *Data-independent acquisition*). All `characteristics[...]`/`comment[...]` columns used are licensed by one of these five declared templates.

## Scope — deposited vs annotated (reconciled)
- **Authoritative file list:** `https://www.ebi.ac.uk/pride/ws/archive/v3/projects/PXD054083/files/all` → **201 entries; every entry's `projectAccessions` contains PXD054083** (asserted programmatically, 0 violations). Breakdown: **186 RAW**, 9 SEARCH (Spectronaut report `.tsv`), 6 OTHER (5 Spectronaut `.setup` txt + `checksum.txt`).
- **All 186 deposited raw runs are annotated** (186 rows). `comment[data file]` set == the full RAW set (verified 186/186). No raw run excluded; no non-raw run carries an independent sample.
- MCP `get_project_files` was **not** used for the count (truncates at 100) — the REST `files/all` JSON was authoritative.

### Sample breakdown (186 rows)
| Cell line | Cellosaurus | single cell (1c) | bulk (10c/20c/pool) | total |
|---|---|---|---|---|
| HeLa | CVCL_0030 | 40 | 9 (20c) | 49 |
| HEK293T | CVCL_0063 | 44 | 47 (10c/20c/pool) | 91 |
| hFF (foreskin fibroblast) | not available | 0 | 46 (bulk) | 46 |

Treatment arms (verified against preprint Figs 2–4 + filename tokens):
- **HEK293T:** bortezomib (1c ×12, 10c ×12, pool ×6), cycloheximide (1c ×12, 10c ×12, pool ×5), untreated (1c ×20, 10c ×6, 20c ×3, pool ×3).
- **HeLa:** CHIR99021 (1c ×12, 20c ×3); `not applicable` (1c ×28, 20c ×6) — of which **15 rows are the DMSO vehicle `_Control_` runs paired with the CHIR arm** and 19 are the plain-untreated `20231122` HeLa runs (see reviewer point 1).
- **hFF:** 46 bulk SILAC-incorporation time-course/benchmark runs, no drug.

## Key decisions and evidence (independently re-verified this run)
- **SILAC labels (stated, not guessed):** preprint Methods — *"heavy SILAC L-lysine-¹³C₆¹⁵N₂ and L-arginine-¹³C₆¹⁵N₄"* (Lys +8 / Arg +10); PRIDE `data_processing_protocol` — *"channel2 with Lys8 and Arg10."* Encoded as **variable** `comment[modification parameters]`: `NT=Label:13C(6)15N(2);AC=UNIMOD:259;TA=K;MT=Variable` and `NT=Label:13C(6)15N(4);AC=UNIMOD:267;TA=R;MT=Variable`. **UNIMOD:259 / 267 verified live via OLS.** `comment[label] = SILAC` (one value per run — the light/heavy pair is the intra-cell pulse readout of a **single** sample, not two samples).
- **No Carbamidomethyl / no alkylation:** `data_processing_protocol` — *"carbamidomethylation was not included in the fixed modifications"*; preprint digestion (0.2 % DDM-class buffer, TEAB, lysyl endopeptidase + trypsin) documents **no reduction/alkylation step**. The reflex Carbamidomethyl annotation was deliberately omitted (would be wrong on every row).
- **Other search mods:** Spectronaut defaults Oxidation (M, **UNIMOD:35**) and Acetyl (Protein N-term, **UNIMOD:1**), both variable; corroborated by PRIDE `modifications` (*monohydroxylated residue / acetylated residue*). UNIMOD:1 = Acetyl, UNIMOD:35 = Oxidation confirmed (Acetyl ≠ Phospho, no swap).
- **Enzymes:** dual **Lys-C + Trypsin** → two `comment[cleavage agent details]` columns (`MS:1001309`, `MS:1001251`), both OLS-verified.
- **Cell-line identity (preprint Methods, line "HeLa, HEK 293T and hFF cells (ATCC)"):**
  - **HeLa → CVCL_0030** (parental HeLa; Cellosaurus, `disease = cervical adenocarcinoma` MONDO:0005153, `organism part = uterine cervix` UBERON:0000002).
  - **HEK293T → CVCL_0063** — the paper explicitly writes **"HEK 293T"** (5 occurrences), **not** plain HEK293. The `HEK293/CVCL_0045` trap was actively avoided: Cellosaurus confirms CVCL_0063 = HEK293T vs CVCL_0045 = HEK293. `organism part = kidney` UBERON:0002113 (female, `disease = not applicable`).
  - **hFF** — verified from the **paper, not the filename token** (`FIBRO`): preprint states *"human foreskin fibroblasts (hFF)"* / *"homogeneous human foreskin (hFF) cell line"* (ATCC), with **no catalog/clone number** → specific Cellosaurus line **not determinable** → `cellosaurus accession/name = not available`, `cell line = hFF`, `organism part = prepuce of penis` UBERON:0001332, `sex = male`, `cell type = fibroblast` CL:0000057.
- **Drugs (OLS-verified, smart-mode traps avoided):** **bortezomib = NCIT:C1851** (not the "Bortezomib/Rituximab Regimen" smart hit), **cycloheximide = NCIT:C171712**. **CHIR99021** (GSK3 inhibitor) kept as informative free text — no NCIT/EFO class exists (only CHEBI:91091, disallowed by the column grammar).
- **Instrument:** **Orbitrap Astral = MS:1003378** (OLS-verified). Acquisition `Data-independent acquisition`; `dissociation method = beam-type collision-induced dissociation` MS:1000422; DIA windows `380 / 980 / 4`, `27 NCE`.
- **Isolation / counts:** single cells → `single cell isolation protocol = cellenONE`, `cells per well = 1`, `sample type = single cell`; multi-cell pools (10/20) and bulk → `sample type = bulk control`, exact count preserved in `cells per well` (or `not applicable` for undiluted bulk).

**All 18 distinct ontology accessions in the file were verified live** (OLS4 REST + Cellosaurus): UNIMOD:1/35/259/267, MS:1000422/1001251/1001309/1003378, UBERON:0000002/0001332/0002113, CL:0000057/0000066, MONDO:0005153, CVCL_0030/0063, NCIT:C1851/C171712.

## Inferred vs stated
- **Stated:** SILAC reagents/residues, no-CAA, enzymes, instrument, DIA acquisition, cell lines (ATCC: HeLa/HEK293T/hFF), drug identities & concentrations, cellenONE isolation, HCD 27 % NCE.
- **Inferred:** biological-replicate index (running number within each cell-line × condition × cells-per-well group); grouping of 10/20-cell pools under `sample type = bulk control`; classification of each run's arm from the raw filename token (`HEK`, `Hela`/`HeLa`, `FIBRO`, `borte`, `cyclo`, `CHIR`, `Control`, `Nc`).

## Values left `not available` / `not applicable` — and why
- `age` → **not available** (all): donor ages are cell-line-marginal, not a reliable per-sample property.
- `cellosaurus accession / name` → **not available** for hFF (unnamed ATCC foreskin line, no catalog number).
- `cell type` → **not available** where the line lacks a single clean CL term at the needed granularity (kept broad rather than over-specific).
- `disease` → **not applicable** for HEK293T and hFF (non-cancer); HeLa = cervical adenocarcinoma.
- `sex` → present where the line's donor sex is established (HeLa/HEK293T female, hFF male).
- Drug **concentrations** (1/10 µM bortezomib; 2/20 µM cycloheximide) are preserved in `source name` / `cell identifier` — there is no clean SDRF ontology column for dose.
- **No `characteristics[cell line]` cell is a reserved word** — every one of the 186 rows is a named cultured line (HeLa/HEK293T/hFF), so the #35 B1 cell-lines reserved-word trap **does not arise**; a file-wide `cell-lines` declaration is safe here (no primary/no-cell-line rows).

## Conflicts found and how resolved
- **PRIDE `publications` empty** → resolved via title/author match to PMID 40168994 + bioRxiv preprint (same as sibling).
- **Filename token `HEK` vs paper `HEK 293T`** → paper wins (precedence: paper Methods > filename); annotated HEK293T/CVCL_0063.
- **Filename token `FIBRO` vs paper `human foreskin fibroblast`** → paper wins; hFF, no specific Cellosaurus line.
- **No cross-dataset checksum possible** (PXD051942 ships no `checksum.txt`) → duplication proven instead by 186/186 exact filename match **and** 186/186 byte-identical `fileSizeBytes` from both `files/all` JSONs, plus shared identical Spectronaut report/setup files in both deposition folders and PXD054083's own `..._non-EB_files_Px_submission` path label. See DUPLICATES Group 6.

## Validation
`parse_sdrf validate-sdrf --sdrf_file annotations/PXD054083.sdrf.tsv --template ms-proteomics --template human --template cell-lines --template single-cell --template dia-acquisition` (sdrf-pipelines 0.1.5) → **exit 0, 0 ERRORs.**

**Accepted warning (justified):**
- `treatment: CHIR99021 not in ncit;efo` — no NCIT/EFO class exists; the compound is only in CHEBI (CHEBI:91091), which the column grammar disallows. Kept as informative free text rather than dropping to `not available`.

`parse_sdrf` exit 0 proves well-formedness only (it silently accepts bogus accessions and does not enforce `allow_*` reserved-word flags); every accession here was **additionally verified live via OLS/Cellosaurus**, and reserved-word usage was checked against the resolved templates.

## Points a reviewer should attack hardest
1. **DMSO vehicle controls annotated `treatment = not applicable`.** 15 HeLa rows are the `_Control_` runs the paper explicitly calls the **DMSO-treated control** for the CHIR99021 arm. I annotated them `not applicable` **to stay byte-consistent with the approved PXD051942 rows for these identical runs**, but a reviewer may reasonably prefer `treatment = DMSO` (the property arguably *does* apply to a vehicle control). Because these are the **same physical runs** in both accessions, any change should be applied to **both** PXD054083 and PXD051942 together to avoid cross-dataset disagreement.
2. **SILAC representation:** one row/file with `comment[label]=SILAC` + heavy labels as variable mods, vs. the classic two-row (SILAC light/heavy) explosion. Chosen because both channels are the intra-cell pulse of a **single** sample (per brief); a quantms-oriented reviewer may prefer two rows sharing one cell identity.
3. **`sample type = bulk control`** (nearest valid PRIDE term) applied to 10/20-cell pools that are not literally "bulk"; exact count is in `cells per well`.
4. **hFF Cellosaurus line** left `not available` (ATCC catalog not stated) — could a specific line be inferred?
5. **Biological-replicate numbering** scheme (per-group running index) and reuse of the sibling's `source name` strings (which carry a cell-line prefix rather than the raw-file prefix).

## Duplicate cross-reference
See **`annotations/DUPLICATES.md` → Group 6 (PXD054083 ⊂ PXD051942)**. Canonical recommendation for corpus assembly: **PXD051942** (superset; contains these 186 runs plus the 1,190 iPSC/EB runs).

*Producer self-assessment only — not an approval. A separate adversarial reviewer must gate this artifact.*
